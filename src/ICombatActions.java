
public interface ICombatActions { //Interfaces used to force characters to have ways to fight

	void normalAttack(); // Used to perfom a normal attack
	void specialAttack(); // Used to perform an ability (Skill/Spell)
}
